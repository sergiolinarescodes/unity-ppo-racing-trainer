[tool.ruff]
line-length = 120
extend-exclude = ["tools/python"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.per-file-ignores]
# Dashboard inlines the HTML/CSS/JS UI as long string literals.
"tools/dashboard/server.py" = ["E501"]
